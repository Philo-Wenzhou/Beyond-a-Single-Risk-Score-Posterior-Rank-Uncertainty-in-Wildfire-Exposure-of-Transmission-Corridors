# Project Memory

See the root project memory file:

```text
PROJECT_MEMORY.md
```

This document exists so the project memory is visible from both the project
root and the `docs/` index. Keep the root file as the canonical memory source.
